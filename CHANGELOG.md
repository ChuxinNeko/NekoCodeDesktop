# 更新日志

## v0.0.4

### 新功能

- **图片输入**：输入框可直接附加图片（PNG / JPEG / WebP / GIF / BMP，每条消息最多 4 张、单张不超过 10 MiB、合计不超过 20 MiB），支持自动压缩；供应商设置中新增"支持图片输入"开关，仅对实际支持多模态的模型启用。
- **Fast Context**：`code_search` 只读探索子代理可单独配置模型与思考等级；自动模式在已配置 Fusion 时使用 Sidekick，否则跟随当前模型；用量统计中单独列出 Fast Context 消耗。
- `code_search` 工具已注册到 agent / ask / debug / multitask / plan 各模式的提示词中。

### 性能改进

- **长会话加载**：会话快照改为窗口化传输，打开长会话时先加载尾部内容，向上滚动时按需取回更早的记录。
- **渲染优化**：快照跨 IPC 传输后通过结构共享复用未变化的引用，流式输出时不再重渲染已稳定的转录内容。

### 其他

- 图片附件与 Fast Context 配置同步到 WebUI、局域网与移动端连接。
- 修复与改进任务管理、工作流运行时、融合用量统计、模型配置存储、提示词库等配套逻辑。
